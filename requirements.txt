# requirements.txt — RUNTIME dependencies only (P0-4).
# Dev/notebook tooling lives in requirements-dev.txt; CI installs
# requirements-ci.txt. Pins marked EXACT were taken from the working
# environment's pip freeze; the ~= entries are compatible-release pins —
# after installing, run `pip freeze` and tighten them to == for full
# reproducibility.

numpy==2.4.6
matplotlib==3.10.9
rasterio==1.4.4
requests==2.34.2
scipy~=1.16
pyproj~=3.7          # P0-4: imported by coordinate_conversion_pixel; previously undeclared
psycopg2-binary~=2.9
python-dotenv~=1.1
pydantic-settings~=2.10   # P0-3 settings layer
folium~=0.20
reportlab~=4.4
pandas~=2.3          # setup scripts + labelling export
ijson~=3.4           # boundary GeoJSON streaming in setup_boundaries
